# kasparro-agentic-fb-analyst-vidhi-rawal

Agentic Facebook Ads Performance Analyst — a multi-agent pipeline that diagnoses ROAS changes, validates hypotheses quantitatively, and generates creative recommendations grounded in campaign creative messaging.

## Quick start

1. Create venv and install dependencies
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
